<main class="form-page">  
  <div class="chirpy-form">    
    <h2><%= t('.sign_up', :default => "Sign up") %></h2>

    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div id="email"><%= f.label t('.Email') %><br />
      <%= f.email_field :email %></div>

      <div><%= f.label t('.Password') %><br />
      <%= f.password_field :password %></div>

      <div><%= f.label t('.Password_Confirmation') %><br />
      <%= f.password_field :password_confirmation %></div>

      <div><%= f.submit t('.sign_up', :default => "Sign up"), class: 'white btn' %></div>
    <% end %>
  </div>
</main>    
